{% extends 'base' %}

{% block content %}
  <h2>Categories</h2>
  <ul>
    {% for category in store.categories %}
      <li>
        <a href="/categories/{{ category.handle }}">{{ category.name }}</a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
